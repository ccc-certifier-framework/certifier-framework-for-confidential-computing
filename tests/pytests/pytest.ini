[pytest]
markers =
    check_leaks: Check for memory leaks using --memray package. (deselect with '-m "not check_leaks"')
    needs_cert_service: Test case that needs Certifier Service to be up-and-running.
